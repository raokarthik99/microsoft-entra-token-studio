# =============================================================================
# Entra Token Studio - Environment Configuration
# =============================================================================
# Copy this file to .env for basic server configuration.
# Never commit .env to version control.

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Port for the dev server (defaults to 5173)
PORT=5173

# Redirect URI for user token authentication callbacks
# Override this if running on a different domain or port
REDIRECT_URI=http://localhost:5173/auth/callback

# =============================================================================
# APP CONFIGURATION
# =============================================================================
# App credentials are configured through the UI and stored in Azure Key Vault.
#
# To add your first app:
#   1. Open the app at http://localhost:5173
#   2. You'll be redirected to the Setup page
#   3. Click "Add Your First App"
#   4. Enter your Entra app details and Key Vault configuration
#
# Required for each app:
#   - Tenant ID (from Azure Entra > App registrations)
#   - Client ID (from Azure Entra > App registrations)
#   - Azure Key Vault URI (e.g., https://your-vault.vault.azure.net)
#   - Secret or Certificate name in Key Vault
