# =============================================================================
# Entra Token Client - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# =============================================================================
# CORE CONFIGURATION (Required)
# =============================================================================

# Your Azure AD tenant ID (Directory ID)
TENANT_ID=00000000-0000-0000-0000-000000000000

# Your app registration client ID (Application ID)
CLIENT_ID=00000000-0000-0000-0000-000000000000

# Port for the dev server (optional, defaults to 5173)
PORT=5173

# Redirect URI for authentication callbacks
# Must match exactly what's configured in your Entra app registration
REDIRECT_URI=http://localhost:5173/auth/callback

# =============================================================================
# AUTHENTICATION METHODS
# =============================================================================
# Choose ONE method below. Priority order (highest to lowest):
#   1. Key Vault Certificate
#   2. Local Certificate
#   3. Key Vault Secret
#   4. Environment Variable Secret

# -----------------------------------------------------------------------------
# OPTION 1: Client Secret (Environment Variable)
# -----------------------------------------------------------------------------
# Simplest option, good for local development
# Get this from: Entra portal > App registrations > Your app > Certificates & secrets

CLIENT_SECRET=your-client-secret-value

# -----------------------------------------------------------------------------
# OPTION 2: Local Certificate File
# -----------------------------------------------------------------------------
# Use a local .pem or .pfx file containing both certificate and private key
# The public key (.cer) must be uploaded to your Entra app registration

# CERTIFICATE_PATH=/absolute/path/to/certificate.pem
# CERTIFICATE_PFX_PASSPHRASE=           # Optional: passphrase for encrypted PFX files

# -----------------------------------------------------------------------------
# OPTION 3: Azure Key Vault - Secret
# -----------------------------------------------------------------------------
# Store your client secret in Key Vault instead of environment variables
# Requires: Azure CLI login or managed identity with Key Vault Secrets User role

# AZURE_KEYVAULT_URI=https://your-vault-name.vault.azure.net
# AZURE_KEYVAULT_SECRET_NAME=your-secret-name

# -----------------------------------------------------------------------------
# OPTION 4: Azure Key Vault - Certificate (Recommended for Production)
# -----------------------------------------------------------------------------
# Most secure option - certificate with private key stored in Key Vault
# Requires:
#   - Certificate imported to Key Vault with private key
#   - Public key uploaded to Entra app registration
#   - Azure CLI login or managed identity with:
#     * Key Vault Certificates User role, AND
#     * Key Vault Secrets User role (to read private key)

# AZURE_KEYVAULT_URI=https://your-vault-name.vault.azure.net
# AZURE_KEYVAULT_CERT_NAME=your-certificate-name
